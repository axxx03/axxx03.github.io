# 博客启动说明

## 🚀 快速启动（推荐）

### 方式一：自动检测（推荐）

在项目根目录运行：

```bash
./start.sh
```

脚本会自动检测 Docker 是否可用，如果 Docker daemon 未运行，会提示你启动 Docker Desktop。

### 方式二：明确指定启动方式

```bash
./start.sh docker    # 使用 Docker 方式（需要 Docker Desktop 运行）
./start.sh local     # 使用本地 Ruby 方式（需要 Ruby >= 3.0）
./start.sh auto      # 自动选择可用方式
```

**重要提示**: 
- 由于系统 Ruby 版本为 2.6.10，而项目需要 Ruby >= 3.0，**强烈建议使用 Docker 方式**
- 使用 Docker 前，请确保 **Docker Desktop 已启动**
- 如果看到 "Docker daemon 未运行" 错误，请先启动 Docker Desktop 应用

## 启动方式说明

### Docker 方式（推荐）
- **前置条件**: 需要先安装并启动 Docker Desktop
  - macOS: 打开 "Docker Desktop" 应用
  - 等待 Docker 图标在菜单栏显示为运行状态
- 自动构建镜像并启动容器
- 访问地址: http://localhost:4000
- 查看日志: `docker-compose logs -f`
- 停止服务: `docker-compose down`

**如果遇到 "Docker daemon 未运行" 错误**:
1. 打开 Docker Desktop 应用
2. 等待 Docker 完全启动（菜单栏图标不再闪烁）
3. 再次运行 `./start.sh docker`

### 本地方式
- 需要系统已安装 Ruby
- 自动检查并安装 Bundler
- 自动安装项目依赖
- 访问地址: http://localhost:4000
- 按 `Ctrl+C` 停止服务器

## 常见问题

1. **如果遇到权限问题**
   - 使用 Docker 方式启动
   - 或安装 Homebrew Ruby: `brew install ruby`

2. **如果端口 4000 被占用**
   - 脚本会自动尝试停止占用端口的进程
   - 或手动停止: `lsof -ti:4000 | xargs kill -9`

3. **首次启动较慢**
   - 需要安装依赖，请耐心等待
   - 后续启动会更快

## 📊 检查博客状态

运行检查脚本查看博客运行状态：

```bash
./check.sh
```

这会显示：
- 端口占用情况
- Docker 容器状态
- HTTP 响应状态
- 最新日志

## 🛑 停止博客

- Docker 方式: `docker-compose down`
- 本地方式: 在运行脚本的终端按 `Ctrl+C`
- 强制停止: `lsof -ti:4000 | xargs kill -9`

## 📝 下次启动步骤

1. **进入项目目录**:
   ```bash
   cd /Users/rongwei/Note/academicpages.github.io
   ```

2. **启动博客**:
   ```bash
   ./start.sh
   ```
   或
   ```bash
   ./start.sh docker
   ```

3. **等待启动完成**（首次启动可能需要几分钟构建 Docker 镜像）

4. **访问博客**: 打开浏览器访问 http://localhost:4000

5. **检查状态**（可选）:
   ```bash
   ./check.sh
   ```

6. **查看日志**（如果需要）:
   ```bash
   docker-compose logs -f
   ```

7. **停止博客**（完成后）:
   ```bash
   docker-compose down
   ```

